.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Data files — regenerate with `python generator/generate_orders.py`
raw/
warehouse/*.duckdb
warehouse/*.duckdb.wal

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
logs/

# Airflow (mounted volumes)
airflow/logs/
airflow/plugins/

# Local tool state (oh-my-claudecode session files)
.omc/

# Regenerable bulk outputs — the markdown summaries stay tracked
reports/*.csv

# OS / editor
.DS_Store
.idea/
.vscode/
